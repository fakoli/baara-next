{
  "name": "@baara-next/root",
  "private": true,
  "workspaces": ["packages/*", "tests"],
  "scripts": {
    "start": "bun run packages/cli/src/index.ts start",
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "test:smoke": "bun test --max-concurrency=1 tests/smoke/",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "^1.3.11",
    "turbo": "^2",
    "typescript": "^5.8"
  }
}
