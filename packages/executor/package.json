{
  "name": "@baara-next/executor",
  "version": "0.1.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2",
    "@baara-next/core": "workspace:*",
    "@extism/extism": "^2.0.0-rc13"
  },
  "devDependencies": {
    "typescript": "^5.8"
  }
}
