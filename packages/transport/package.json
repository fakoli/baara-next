{
  "name": "@baara-next/transport",
  "version": "0.1.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@baara-next/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8"
  }
}
